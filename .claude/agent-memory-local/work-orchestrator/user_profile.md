---
name: user-profile
description: Zozi — author of the agents-toolkit / token-efficient-repo-work plugin; works in Spanish; prefers autonomous full implementation
metadata:
  type: user
---

Zozi (GitHub: Zozi96) authors the `token-efficient-repo-work` plugin in this repo, targeting both Claude Code and Codex. Communicates in Spanish; replies should be in Spanish.

**How to collaborate:** When offered a ranked proposal, responds "implementa todo lo que se considere adecuado" — expects autonomous end-to-end implementation (code + sync + tests + docs + CI) without further check-ins, including reasonable design decisions (e.g., choosing deny-with-replacement over soft hints). Cares about cross-platform parity (POSIX + Windows PowerShell) and token efficiency without adding agent-workflow friction.
